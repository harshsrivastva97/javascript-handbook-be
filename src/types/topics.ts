export interface TopicDetails {
    topic_id: number;
    title: string;
    explanation: string;
    code_example: string;
    key_points: string[];
}

export interface Topic {
    topic_id: number;
    title: string;
}
